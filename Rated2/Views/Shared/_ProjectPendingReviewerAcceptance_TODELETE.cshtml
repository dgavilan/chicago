@model List<Rated2.Models.Project.ProjectViewModel>
@using Rated.Core.Shared

<table class="table table-striped">
    <thead>
        <tr>
            <th width="60%">Project</th>
            <th>Modified</th>
            <th>Status</th>
        </tr>
    </thead>
    
    @foreach (var project in Model)
    {
        var statusId = (int)project.ProjectStatus;
        <tr>
            <td>
                <a onclick="window.location='/Project/ViewReview/@project.ProjectId/?ProjectStatusId=@statusId&Review=1'">
                    <span class="glyphicon glyphicon-list"></span>
                    View Details
                </a>

                

                <br />
                <b>@project.ProjectName</b>
                <br />
                @project.ProjectDescription
            </td>
            <td width="100px">
                @project.ModifiedDate
            </td>
            <td>
                @Rated.Core.Shared.EnumHelper.GetDescription(project.ProjectStatus)
            </td>
        </tr>
    }
</table>